services:
  - type: web
    name: contabilapp-verificacion
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python servidor_render.py
    envVars:
      - key: GOOGLE_CREDS_JSON
        sync: false
        
        

